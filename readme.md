Create a new folder named "Portfolio-Project"
open the folder in vs code
open the terminal in vs code


//backend
create a new folder named "server"
run the command "npm init -y"
in package.json file add "type": "module"
create a file "index.js" in the "server" folder

terminal: npm i mongoose dotenv cors nodemon express 
import the libraries in the index.js file
add script to run index.js using nodemon



//frontend
run npm create vite@latest
enter the name "client"
press enter until the project is made 

create a components folder
create three files: register-login-dashboard
then run rafce to get the boiler plate code in all three files

then create a basic login page# mern-project-dec5
